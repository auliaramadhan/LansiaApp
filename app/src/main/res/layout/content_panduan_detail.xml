<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".PanduanDetailActivity"
    android:background="@drawable/background_gradient"
    tools:showIn="@layout/activity_panduan_detail">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/img_lana"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:layout_marginBottom="32dp"
        app:srcCompat="@drawable/lana" />

    <TextView
        android:id="@+id/titlepanduan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:layout_marginBottom="16dp"
        android:padding="8dp"
        android:fontFamily="@font/textfont"
        android:text="Cara Menggunakan Lapotik"
        android:background="@color/colorButton"
        android:textColor="@color/colorTextButton"
        android:textSize="18sp"
        android:clickable="true"/>

    <ImageView
        android:id="@+id/imagepanduan"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:layout_gravity="center" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/detailpanduan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/panduanLapotik"
            style="@style/word_beranda"
            android:layout_gravity="center|top"
            android:textSize="24sp"
            />
    </ScrollView>


</LinearLayout>